// @flow
import { stateToGetters } from '../common/util'

type State = {
}

const state: State = {
}

export default {
    namespaced: true,
    state,
    getters: stateToGetters(state),
    mutations: {
    },
    actions: {
    }
}
