// @flow
import { stateToGetters } from '../common/util'

type State = {
}

const state: State = {
}

export default {
    state,
    getters: stateToGetters(state),
    mutations: {
    },
    actions: {
    }
}
